- defaults:
    link:
      relink: true
      force: true

- clean: ['~']

- shell:
  - git config --global core.editor "vim"
  - git config --global user.name "Jeff Hentschel"

- link:
    ~/.zshrc: zshrc
    ~/.vim: vim

- create:
    - ~/git
    - ~/git/jeffaudio

